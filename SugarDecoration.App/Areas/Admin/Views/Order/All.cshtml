@model AllOrdersQueryModel
@{
    ViewBag.Title = "Всички поръчки";
}

<div class="container">
    <h2>@ViewBag.Title</h2>
    <ul class="list-group">
        @if (Model.Orders.Any())
        {
            @foreach (var order in Model.Orders)
            {
                <li class="list-group-item">
                    <div class="cart-item">
                        <div>
                            <p>Дата на поръчване: @order.OrderDate</p>
                            <p>Общо продукти: @order.TotalItemCount</p>
                        </div>
                        <a class="btn btn-primary" asp-area="Admin" asp-controller="Order" asp-action="Delete">Изтрий</a>
                        <a class="btn btn-primary" asp-area="Admin" asp-controller="Order" asp-action="Details">Детайли</a>
                    </div>
                </li>
            }double[] arr = Console.ReadLine().Split
        }
        else
        {

            <p>
                В момента няма поръчки.
            </p>
        }

    </ul>
</div>


<style>
    .cart-item {
        display: flex;
        align-items: center;
        border-bottom: 1px solid #dee2e6;
        padding: 10px;
    }

        .cart-item img {
            max-width: 100px;
            margin-right: 20px;
        }
</style>